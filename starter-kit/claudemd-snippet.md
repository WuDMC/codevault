## Memory Integration

This project uses Memory MCP for persistent context across sessions.
See `.claude/skills/memory-agent/SKILL.md` for the full protocol.

**TL;DR**: Load context at session start. Save decisions before session end.
